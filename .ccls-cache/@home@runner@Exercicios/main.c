#include <stdio.h>

int main(void) {
 int numeros;

  printf("Digite a Quantidade de números do Array (limite de 4 números):");
  scanf("%d", &numeros);

int Array[numeros];

printf("Digite os números do Array:", numeros);
  for (int i = 0; i < numeros; i++){
    
  }
}